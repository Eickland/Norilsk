#!/bin/bash

# Останавливаем выполнение при любой ошибке
set -e

echo "--- 🚀 Начало процесса обновления ---"

# 1. Затягиваем последние изменения из Git
echo "📥 Получение обновлений из репозитория..."
git pull origin main

# 2. Пересобираем и запускаем контейнеры
echo "🏗️ Пересборка и запуск контейнеров..."
# --build заставит Docker пересобрать образ Flask с новым кодом
# -d запустит всё в фоновом режиме
sudo docker compose up -d --build

# 3. Очистка старых данных
echo "🧹 Удаление неиспользуемых образов (очистка места)..."
# Удаляет образы, которые остались после пересборки (dangling images)
sudo docker image prune -f

echo "✅ Обновление успешно завершено!"
echo "Статус контейнеров:"
sudo docker compose ps