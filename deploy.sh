#!/bin/bash

set -e

echo "--- 🚀 Начало процесса обновления ---"

# Сохраняем текущую базу данных (если существует)
if [ -f "data/data.json" ]; then
    echo "💾 Сохранение текущей базы данных..."
    cp data/data.json /tmp/data_backup.json
fi

echo "📥 Получение обновлений из репозитория..."
git pull origin main

# Восстанавливаем базу данных после обновления
if [ -f "/tmp/data_backup.json" ]; then
    echo "🔄 Восстановление базы данных..."
    mkdir -p data
    cp /tmp/data_backup.json data/data.json
    rm /tmp/data_backup.json
fi

echo "🏗️ Пересборка и запуск контейнеров..."
sudo docker compose up -d --build

echo "🧹 Удаление неиспользуемых образов..."
sudo docker image prune -f

echo "✅ Обновление успешно завершено!"
echo "Статус контейнеров:"
sudo docker compose ps